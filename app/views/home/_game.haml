.game
  .game-items
    %h3= game[:name]
    %span.game-info{ :hidden => '', 'data-id' => game[:id] }
    - items.each do |item|
      = render 'item', :item => item
  .game-chart{ :id => "container#{game[:id]}" }
